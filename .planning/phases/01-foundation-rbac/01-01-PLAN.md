---
wave: 1
depends_on: []
files_modified:
  - backend/app/models/user.py
  - backend/app/schemas/auth.py
  - backend/app/core/deps.py
  - backend/tests/test_auth.py
  - backend/database/seed.py
autonomous: true
---

# Plan 01-01: Update User Roles and Implement RBAC Dependencies

## Goal
Establish the correct system roles (`admin`, `doctor`, `asha_worker`, `migrant_worker`) and enforce them across standard endpoints.

## Requirements
- AUTH-02: ASHA worker / Doctor can login via Email and Password.
- AUTH-03: System provisions JWT access tokens with granular role scopes.

## Tasks

```xml
<task>
  <description>Update the UserRole enum to match SwasthyaSetu requirements.</description>
  <read_first>
    - backend/app/models/user.py
    - backend/app/schemas/auth.py
  </read_first>
  <action>
    Modify `UserRole` in `backend/app/models/user.py` and any related mock seeds to use only these exact 4 values:
    - `admin`
    - `doctor`
    - `asha_worker`
    - `migrant_worker`
    
    If there are database seeders (`backend/database/seed.py`), update their mock users to use `admin` and `doctor` roles instead of `govt` or `clinic`.
  </action>
  <acceptance_criteria>
    - `backend/app/models/user.py` contains `admin = "admin"` and `asha_worker = "asha_worker"` inside `UserRole`.
    - `pytest backend/tests/ -v` passes cleanly without enum parsing errors.
  </acceptance_criteria>
</task>

<task>
  <description>Generate an Alembic migration for the enum change (if postgres).</description>
  <read_first>
    - backend/alembic.ini
  </read_first>
  <action>
    Run `alembic revision --autogenerate -m "update user roles"` and ensure the migration script correctly alters the `userrole` ENUM type in Postgres. Since altering Enums in Postgres automatically is tricky, manually inspect the generated migration file and adjust the `op.execute` statements if necessary to replace the old enum with the new one.
  </action>
  <acceptance_criteria>
    - A new file exists in `backend/alembic/versions/` referencing the role update.
  </acceptance_criteria>
</task>

<task>
  <description>Implement RBAC dependencies in FastAPI.</description>
  <read_first>
    - backend/app/core/deps.py
    - backend/app/routes/auth.py
  </read_first>
  <action>
    In `backend/app/core/deps.py`, add a `RoleChecker` dependency class or function that takes a list of `UserRole`s and returns a dependency validating `current_user.role`.
    Create a secure test endpoint `/auth/admin-only` in `backend/app/routes/auth.py` that requires the `admin` role.
  </action>
  <acceptance_criteria>
    - `backend/app/core/deps.py` contains a dependency that checks roles.
    - `backend/app/routes/auth.py` contains `@router.get("/admin-only")`.
  </acceptance_criteria>
</task>

<task>
  <description>Write tests proving RBAC enforces access.</description>
  <read_first>
    - backend/tests/test_auth.py
  </read_first>
  <action>
    Add a test `test_admin_only_route_rejects_doctor` to `backend/tests/test_auth.py`. Show that a user with the `doctor` role gets a 403 Forbidden when calling `/auth/admin-only`, and an `admin` user gets a 200 OK.
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/test_auth.py` passes.
    - Test asserts `status_code == 403` for unauthorized role.
  </acceptance_criteria>
</task>
```

## Verification
- Run `pytest backend/tests/` and confirm all tests pass.
- Start the server and manually check the `admin-only` route with a non-admin token to verify the 403 response.

## Must Haves
- UserRole enum perfectly matches the PRD (`admin`, `doctor`, `asha_worker`, `migrant_worker`).
- RBAC is provably enforced on at least one endpoint.
