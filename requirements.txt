# =============================================================================
# SwasthyaSetu — Python Backend Requirements
# AI-Powered Portable Healthcare Ecosystem for Migrant Workers
# =============================================================================
# Python Version: 3.10+
# Install: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# Web Framework & Server
# -----------------------------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0
python-multipart==0.0.9          # File uploads (document scans)
httpx==0.27.0                    # Async HTTP client (external API calls)
starlette==0.37.2

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
python-jose[cryptography]==3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4            # Password hashing
cryptography==42.0.8              # AES-256 field-level encryption
python-dotenv==1.0.1              # Environment variable management
authlib==1.3.1                    # OAuth2 (ABHA integration)
pydantic==2.7.1                   # Data validation
pydantic-settings==2.3.0

# -----------------------------------------------------------------------------
# Database & ORM
# -----------------------------------------------------------------------------
sqlalchemy==2.0.30                # ORM for PostgreSQL
alembic==1.13.1                   # Database migrations
asyncpg==0.29.0                   # Async PostgreSQL driver
psycopg2-binary==2.9.9            # Sync PostgreSQL driver (fallback)
motor==3.4.0                      # Async MongoDB driver
pymongo==4.7.2                    # MongoDB base driver
redis==5.0.4                      # Redis client (caching, sessions)
celery==5.4.0                     # Async task queue (notifications)
kombu==5.3.7                      # Celery message transport

# -----------------------------------------------------------------------------
# AI / ML — Core
# -----------------------------------------------------------------------------
torch==2.3.0                      # PyTorch (deep learning backbone)
torchvision==0.18.0               # Vision utilities (document preprocessing)
torchaudio==2.3.0                 # Audio processing for voice AI
transformers==4.41.2              # HuggingFace Transformers (IndicBERT, mBERT)
datasets==2.19.1                  # HuggingFace datasets (model training)
accelerate==0.30.0                # Multi-GPU / distributed training
sentencepiece==0.2.0              # Tokenizer for Indic NLP models
tokenizers==0.19.1                # Fast tokenizers (HuggingFace)

# -----------------------------------------------------------------------------
# AI / ML — Speech (BhashaSehat)
# -----------------------------------------------------------------------------
openai-whisper==20231117          # Whisper ASR (multilingual speech-to-text)
librosa==0.10.2                   # Audio feature extraction & preprocessing
soundfile==0.12.1                 # Audio file I/O
pyaudio==0.2.14                   # Real-time audio capture
pydub==0.25.1                     # Audio format conversion (mp3/ogg/wav)
noisereduce==3.0.2                # Background noise reduction for field audio

# -----------------------------------------------------------------------------
# AI / ML — NLP (BhashaSehat + Symptom Extraction)
# -----------------------------------------------------------------------------
spacy==3.7.4                      # NLP pipeline (NER, dependency parsing)
# en-core-web-sm                  # Install separately: python -m spacy download en_core_web_sm
indic-nlp-library==0.92           # Indic script normalization & tokenization
langdetect==1.0.9                 # Language auto-detection
googletrans==4.0.0rc1             # Translation fallback (use IndicTrans2 as primary)
sacremoses==0.1.1                 # Moses tokenizer (translation preprocessing)

# -----------------------------------------------------------------------------
# AI / ML — Occupational Health Risk (KaamSuraksha)
# -----------------------------------------------------------------------------
scikit-learn==1.5.0               # ML baseline models
xgboost==2.0.3                    # Gradient boosted trees (risk prediction)
lightgbm==4.3.0                   # LightGBM (ensemble validation)
shap==0.45.1                      # SHAP explainability (risk factor attribution)
imbalanced-learn==0.12.2          # SMOTE for imbalanced disease datasets
optuna==3.6.1                     # Hyperparameter optimization

# -----------------------------------------------------------------------------
# AI / ML — OCR & Document Processing (Document AI)
# -----------------------------------------------------------------------------
opencv-python==4.9.0.80           # Image preprocessing (deskew, denoise, binarize)
pytesseract==0.3.10               # Tesseract OCR wrapper (printed text)
easyocr==1.7.1                    # EasyOCR (semi-handwritten + mixed scripts)
Pillow==10.3.0                    # Image manipulation
pdf2image==1.17.0                 # PDF page rendering for OCR
img2pdf==0.5.1                    # Image to PDF conversion
pypdf==4.2.0                      # PDF metadata and text extraction
scikit-image==0.23.2              # Advanced image processing (thresholding)

# -----------------------------------------------------------------------------
# Data Processing & Analysis
# -----------------------------------------------------------------------------
pandas==2.2.2                     # Data manipulation
numpy==1.26.4                     # Numerical computing
scipy==1.13.0                     # Statistical functions
pyarrow==16.1.0                   # Columnar data format (fast I/O)
openpyxl==3.1.2                   # Excel report generation

# -----------------------------------------------------------------------------
# Geospatial (SehatSetu — ASHA worker assignment)
# -----------------------------------------------------------------------------
geopandas==0.14.4                 # Geospatial data processing
shapely==2.0.4                    # Geometric operations
geopy==2.4.1                      # Geocoding (worker location resolution)
folium==0.16.0                    # Map visualization (worker distribution)

# -----------------------------------------------------------------------------
# External Integrations
# -----------------------------------------------------------------------------
# ABHA / ABDM API
requests==2.32.2                  # HTTP requests to ABHA/ABDM APIs
fhir.resources==7.1.0             # FHIR R4 data models (ABDM compliance)

# WhatsApp Business API
twilio==9.1.0                     # WhatsApp + SMS via Twilio
pywa==1.15.1                      # WhatsApp Cloud API (Meta)

# SMS Gateway
fast2sms==0.1.0                   # Fast2SMS India gateway
# Or use Twilio SMS (included above)

# IVR
plivo==4.45.1                     # Plivo IVR (voice calls for zero-literacy users)

# -----------------------------------------------------------------------------
# Cloud Storage & Infrastructure
# -----------------------------------------------------------------------------
boto3==1.34.115                   # AWS S3 (scanned documents, audio files)
azure-storage-blob==12.20.0       # Azure Blob Storage (alternative)
google-cloud-storage==2.17.0      # GCP Cloud Storage (alternative)

# -----------------------------------------------------------------------------
# Task Queue & Background Jobs
# -----------------------------------------------------------------------------
celery[redis]==5.4.0              # Async tasks (notifications, model inference)
flower==2.0.1                     # Celery task monitoring dashboard
apscheduler==3.10.4               # Scheduled tasks (daily risk score updates)

# -----------------------------------------------------------------------------
# Observability & Logging
# -----------------------------------------------------------------------------
prometheus-client==0.20.0         # Metrics collection
opentelemetry-api==1.24.0         # Distributed tracing
opentelemetry-sdk==1.24.0
loguru==0.7.2                     # Structured logging
sentry-sdk[fastapi]==2.3.1        # Error tracking

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0                     # FastAPI test client (async)
factory-boy==3.3.0                # Test data factories
faker==25.2.0                     # Fake data generation

# -----------------------------------------------------------------------------
# Code Quality
# -----------------------------------------------------------------------------
black==24.4.2                     # Code formatting
ruff==0.4.7                       # Linting (replaces flake8, isort)
mypy==1.10.0                      # Static type checking
pre-commit==3.7.1                 # Git hooks for code quality

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
python-dateutil==2.9.0            # Date/time parsing
pytz==2024.1                      # Timezone handling (IST)
qrcode[pil]==7.4.2                # QR code generation (SwasthyaID)
barcode==1.0.1                    # Barcode generation
uuid==1.30                        # UUID generation
tenacity==8.3.0                   # Retry logic for external API calls
cachetools==5.3.3                 # In-memory caching utilities
ujson==5.10.0                     # Fast JSON parsing

# =============================================================================
# POST-INSTALL STEPS
# =============================================================================
# 1. Install Tesseract OCR (system package):
#    Ubuntu: sudo apt-get install tesseract-ocr tesseract-ocr-hin tesseract-ocr-ben
#    Mac:    brew install tesseract tesseract-lang
#
# 2. Download spaCy model:
#    python -m spacy download en_core_web_sm
#
# 3. Download Whisper model:
#    python -c "import whisper; whisper.load_model('medium')"
#
# 4. Set environment variables (see .env.example):
#    DATABASE_URL, REDIS_URL, MONGODB_URL,
#    ABHA_CLIENT_ID, ABHA_CLIENT_SECRET,
#    TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN,
#    AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, S3_BUCKET_NAME
# =============================================================================
